const error = ({children}) => {
  return (
     <div className="uppercase bg-red-700 text-center p-3 text-white font-bold mb-3 rounded-md">
        {children}
      </div> 
  )
}

export default error
